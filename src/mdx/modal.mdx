---
title: Modal
description: Focused dialog overlays for confirmations and forms.
group: Overlays
slug: modal
---

export const componentCSS = `
.demo-modal-backdrop {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 1000;
    align-items: center;
    justify-content: center;
}
.demo-modal-backdrop.open { display: flex; }
.demo-modal {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    width: 100%;
    max-width: 480px;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.2);
    overflow: hidden;
}
.demo-modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 24px;
    border-bottom: 1px solid var(--border);
}
.demo-modal-header h3 { font-family: var(--font-display); font-size: 18px; font-weight: 700; margin: 0; color: var(--text); }
.demo-modal-close {
    width: 32px;
    height: 32px;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: var(--bg);
    color: var(--text-muted);
    font-size: 16px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.15s, color 0.15s;
}
.demo-modal-close:hover { background: var(--surface); color: var(--text); }
.demo-modal-body { padding: 24px; }
.demo-modal-body p { font-size: 14px; line-height: 1.7; color: var(--text-muted); margin: 0; }
.demo-modal-footer { display: flex; justify-content: flex-end; gap: 10px; padding: 16px 24px; border-top: 1px solid var(--border); }
.demo-btn { font-size: 13px; font-weight: 500; padding: 8px 18px; border-radius: 8px; cursor: pointer; border: 1px solid var(--border); background: var(--bg); color: var(--text); transition: background 0.15s; }
.demo-btn:hover { background: var(--surface); }
.demo-btn-primary { background: var(--accent-500); color: #fff; border-color: var(--accent-600); }
.demo-btn-primary:hover { background: var(--accent-600); }
.modal-trigger-area { display: flex; justify-content: center; padding: 16px; }
`;

export const sections = [
  {
    label: 'Modal',
    demo: `<div class="modal-trigger-area">
  <button class="demo-btn demo-btn-primary" onclick="document.getElementById('demoModal').classList.add('open')">Open Modal</button>
</div>
<div class="demo-modal-backdrop" id="demoModal" onclick="if(event.target===this)this.classList.remove('open')">
  <div class="demo-modal">
    <div class="demo-modal-header">
      <h3>Confirm Action</h3>
      <button class="demo-modal-close" onclick="document.getElementById('demoModal').classList.remove('open')">&#10005;</button>
    </div>
    <div class="demo-modal-body">
      <p>Are you sure you want to delete this item? This action cannot be undone and the data will be permanently removed.</p>
    </div>
    <div class="demo-modal-footer">
      <button class="demo-btn" onclick="document.getElementById('demoModal').classList.remove('open')">Cancel</button>
      <button class="demo-btn demo-btn-primary">Delete</button>
    </div>
  </div>
</div>`,
    tabs: [
      {
        label: 'HTML', lang: 'html', code: `<div class="demo-modal-backdrop" id="myModal">
  <div class="demo-modal">
    <div class="demo-modal-header">
      <h3>Confirm Action</h3>
      <button class="demo-modal-close">&#10005;</button>
    </div>
    <div class="demo-modal-body">
      <p>Are you sure you want to proceed?</p>
    </div>
    <div class="demo-modal-footer">
      <button class="demo-btn">Cancel</button>
      <button class="demo-btn demo-btn-primary">Confirm</button>
    </div>
  </div>
</div>`,
      },
      {
        label: 'CSS', lang: 'css', code: `.demo-modal-backdrop {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 1000;
    align-items: center;
    justify-content: center;
}
.demo-modal-backdrop.open { display: flex; }
.demo-modal {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    width: 100%;
    max-width: 480px;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.2);
    overflow: hidden;
}`,
      },
      {
        label: 'JS', lang: 'js', code: `// Open modal
document.getElementById('openBtn').addEventListener('click', () => {
  document.getElementById('myModal').classList.add('open');
});

// Close modal
document.getElementById('closeBtn').addEventListener('click', () => {
  document.getElementById('myModal').classList.remove('open');
});

// Close on backdrop click
document.getElementById('myModal').addEventListener('click', (e) => {
  if (e.target === e.currentTarget) {
    e.currentTarget.classList.remove('open');
  }
});`,
      },
    ],
  },
];
