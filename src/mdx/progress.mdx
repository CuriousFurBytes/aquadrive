---
title: Progress Bars
description: Visual indicators for task and operation progress.
group: Feedback & Status
slug: progress
---

export const componentCSS = `
.progress-track {
    width: 100%;
    height: 8px;
    background: var(--accent-50);
    border-radius: 100px;
    overflow: hidden;
    margin-bottom: 18px;
}
.progress-track:last-child { margin-bottom: 0; }
.progress-fill {
    height: 100%;
    background: var(--accent-500);
    border-radius: 100px;
    transition: width 0.4s ease;
}
.progress-label {
    display: flex;
    justify-content: space-between;
    font-family: var(--font-mono);
    font-size: var(--text-caption);
    color: var(--text-muted);
    margin-bottom: 6px;
}
.progress-indeterminate .progress-fill {
    width: 40%;
    animation: indeterminate 1.4s ease-in-out infinite;
}
@keyframes indeterminate {
    0%   { transform: translateX(-100%); }
    100% { transform: translateX(350%); }
}
.progress-success .progress-fill { background: var(--green-main); }
.progress-warning .progress-fill { background: var(--yellow-main); }
.progress-error .progress-fill { background: var(--red-main); }
`;

export const sections = [
  {
    label: 'Progress Bars',
    demo: `<div>
  <div class="progress-label"><span>Uploading</span><span>75%</span></div>
  <div class="progress-track"><div class="progress-fill" style="width:75%"></div></div>
  <div class="progress-label"><span>Processing</span><span>40%</span></div>
  <div class="progress-track"><div class="progress-fill" style="width:40%"></div></div>
  <div class="progress-label"><span>Complete</span><span>100%</span></div>
  <div class="progress-track progress-success"><div class="progress-fill" style="width:100%"></div></div>
  <div class="progress-label"><span>Indeterminate</span></div>
  <div class="progress-track progress-indeterminate"><div class="progress-fill"></div></div>
</div>`,
    tabs: [
      {
        label: 'HTML', lang: 'html', code: `<!-- Determinate -->
<div class="progress-label">
  <span>Uploading</span>
  <span>75%</span>
</div>
<div class="progress-track">
  <div class="progress-fill" style="width: 75%"></div>
</div>

<!-- Indeterminate -->
<div class="progress-track progress-indeterminate">
  <div class="progress-fill"></div>
</div>`,
      },
      {
        label: 'CSS', lang: 'css', code: `.progress-track {
    width: 100%;
    height: 8px;
    background: var(--accent-50);
    border-radius: 100px;
    overflow: hidden;
}
.progress-fill {
    height: 100%;
    background: var(--accent-500);
    border-radius: 100px;
    transition: width 0.4s ease;
}
.progress-indeterminate .progress-fill {
    width: 40%;
    animation: indeterminate 1.4s ease-in-out infinite;
}
@keyframes indeterminate {
    0%   { transform: translateX(-100%); }
    100% { transform: translateX(350%); }
}`,
      },
    ],
  },
];
