---
title: Navbar
description: Top navigation bar with links and actions.
group: Navigation
slug: navbar
---

export const componentCSS = `
.demo-navbar {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 12px 20px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
}
.demo-navbar-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    color: var(--text);
    text-decoration: none;
    flex-shrink: 0;
}
.demo-navbar-brand span { color: var(--accent-500); }
.demo-navbar-links { display: flex; align-items: center; gap: 4px; flex: 1; }
.demo-navbar-link {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 6px 12px;
    border-radius: 7px;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.15s, background 0.15s;
}
.demo-navbar-link:hover, .demo-navbar-link.active { color: var(--accent-500); background: var(--accent-50); }
.demo-navbar-actions { display: flex; align-items: center; gap: 8px; margin-left: auto; }
`;

export const sections = [
  {
    label: 'Navbar',
    demo: `<nav class="demo-navbar">
  <a href="#" class="demo-navbar-brand">Aqua<span>Drive</span></a>
  <div class="demo-navbar-links">
    <a href="#" class="demo-navbar-link active">Colors</a>
    <a href="#" class="demo-navbar-link">Typography</a>
    <a href="#" class="demo-navbar-link">Elements</a>
    <a href="#" class="demo-navbar-link">Ports</a>
  </div>
  <div class="demo-navbar-actions">
    <button class="aqua-btn aqua-btn-ghost aqua-btn-sm">Sign in</button>
    <button class="aqua-btn aqua-btn-primary aqua-btn-sm">Get started</button>
  </div>
</nav>`,
    tabs: [
      {
        label: 'HTML', lang: 'html', code: `<nav class="demo-navbar">
  <a href="#" class="demo-navbar-brand">Aqua<span>Drive</span></a>
  <div class="demo-navbar-links">
    <a href="#" class="demo-navbar-link active">Colors</a>
    <a href="#" class="demo-navbar-link">Typography</a>
    <a href="#" class="demo-navbar-link">Elements</a>
  </div>
  <div class="demo-navbar-actions">
    <button>Get started</button>
  </div>
</nav>`,
      },
      {
        label: 'CSS', lang: 'css', code: `.demo-navbar {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 12px 20px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
}
.demo-navbar-link {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 6px 12px;
    border-radius: 7px;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.15s, background 0.15s;
}
.demo-navbar-link:hover,
.demo-navbar-link.active { color: var(--accent-500); background: var(--accent-50); }`,
      },
    ],
  },
];
